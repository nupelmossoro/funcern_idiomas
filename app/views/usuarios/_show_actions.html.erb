<%- model_class=Usuario -%>

<% remote ||=remote %>

 <%= link_to edit_usuario_path(usuario), :class=> 'btn btn-warning me-2', :remote => remote do %>
    <i class="las la-edit"></i> Editar
<% end %>

<%= button_to usuario_path(usuario), :remote=> remote,
    :method => 'delete',
    :data => { :confirm => "Tem certeza que deseja remover?"},
    :class => 'btn btn-danger me-2' do %>
    <i class="las la-trash"></i> Remover
<% end %>

<%= link_to new_usuario_path, class: "btn btn-primary me-2" do %>
    <i class="las la-plus"></i> Adicionar 
<% end %>