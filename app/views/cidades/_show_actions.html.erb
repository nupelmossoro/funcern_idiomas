<%- model_class=Cidade -%>

<% remote ||=remote %>

 <%= link_to edit_cidade_path(cidade), :class=> 'btn btn-warning me-2', :remote => remote do %>
    <i class="las la-edit"></i> Editar
<% end %>

<%= button_to cidade_path(cidade), :remote=> remote,
    :method => 'delete',
    :data => { :confirm => "Tem certeza que deseja remover?"},
    :class => 'btn btn-danger me-2' do %>
    <i class="las la-trash"></i> Remover
<% end %>

<%= link_to new_cidade_path, class: "btn btn-primary me-2" do %>
    <i class="las la-plus"></i> Adicionar 
<% end %>